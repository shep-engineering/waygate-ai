schema_version: "USF v1.0"
name: integrate-waygate_ai
description: Integrate the Waygate AI LLM client into a Python agent or application.
risk_tier: L1
permissions:
  files:
    read:
      - pyproject.toml
      - requirements*.txt
      - "*.py"
    write:
      - .env.example
      - "*.py"
    deny_write:
      - AGENTS.md
      - security/**
  network:
    deny: "*"
